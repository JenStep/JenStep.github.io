#!/bin/bash
#script to build and serve website for testing

echo Building...
bundle exec jekyll build --watch

#check if successful
if [ $? -eq 0 ]; then
	echo Build Succeeded
	echo Serving...
	bundle exec jekyll serve
else
	echo ERROR: Build Failed
fi
